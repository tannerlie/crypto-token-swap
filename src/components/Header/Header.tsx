import { Typography } from "@mui/material";

const Header = () => {
  return (
    <Typography
      fontSize={40}
      fontWeight={700}
      color="#193B4D"
      textAlign={"center"}
      width={"full"}
    >
      EXCHANGE TOKENS
    </Typography>
  );
};

export default Header;
